name: IPA GUI Workflow

# Controls when the workflow will run
on:
  
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on:
    - self-hosted
    - fitqa_node1
    
    steps:
            
                   
      - name: Checkout repo code from GitHub
        uses: actions/checkout@v3  
